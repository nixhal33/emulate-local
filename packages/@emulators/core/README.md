# @emulators/core

Compatibility helpers for the npm-distributed emulate packages. Service routing, state, UI, and protocol behavior now live in the native Go runtime distributed by the `emulate` package.
